let titleOrient = 'right'; // This can be 'right', 'bottom', 'left', 'top', etc.

if (['right', 'bottom'].includes(titleOrient)) {
    // The code inside this block will execute if titleOrient is 'right' or 'bottom'
    console.log("The title orientation is either right or bottom.");
} else {
    // The code inside this block will execute if titleOrient is neither 'right' nor 'bottom'
    console.log("The title orientation is not right or bottom.");
}
