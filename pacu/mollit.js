   for (let i = 0; i < array.length; i++) {
     for (let j = i + 1; j < array.length; j++) {
       if (i >= j - 1) {
         // Perform certain logic when the condition is true
       }
     }
   }
   